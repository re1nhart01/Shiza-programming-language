
// Нада флаг ++native, якщо його немає, то на етапі лексера це нахуй удаляти.
// але при помилці писати, що треба юзати флаг ++native;

cpp (

    
)


js (
    const a = 5;
    const b = a + 12312;
    return b;
)